const { Router } = require("express");

const {
  /*handler */
} = require(/*handler */);

routeRegistroEmpresa = Router();

routeRegistroEmpresa.get("/" /*getRegistroEmpresa*/);

module.exports = routeRegistroEmpresa;
